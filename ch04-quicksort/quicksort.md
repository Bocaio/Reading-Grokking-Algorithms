# Average case Vs Worst Case

QuickSort is O(n^2) time complexity in Worst Case.  
But QuickSort is more often ended up with average case.  
It totally depends on pivot you choose in your algorithm implementation.

To avoid worst case, choose pivot randomly.

## Example
- In this implementation, an array containing all identical elements results in the worst-case O(n^2) behavior.